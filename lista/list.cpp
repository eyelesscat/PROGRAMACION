#include <iostream>
#include <math.h>
#include <conio.h>

using std::endl;
using std::cout;
using std::cin;

int main(){
  int n,cant;
  float var=0, de=0, med=0, sumav=0;
  float lista[cant];

  cout <<"ingresa numeros: ";
  cin >> cant;
  for(num=0; num<cant; num++){
    cin >> lista[num];
  }
  for(num=0, num<cant; num++){
    sumav+=lista[num];
  }
  med=sumav/cant;
  for(num=0; num=cant; num++){
    
  }


  return 0;
}
